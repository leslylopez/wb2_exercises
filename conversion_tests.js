"use strict"
//the variables we are working with
let a = " 101.1 ";
let b = "55";
let c = "402 Stevens";
let d = "Number 5 ";

//using parseInt for each variable and displaying the results 

console.log ("let a with parseInt " + parseInt (a));
console.log ("let b with parseInt " + parseInt (b));
console.log ("let c with parseInt " + parseInt (c));
console.log ("let d with parseInt " + parseInt (d));

//using parseFloat for each variable and displaying the results 

console.log ("let a with parseFloat " + parseFloat (a));
console.log ("let b with parseFloat " + parseFloat (b));
console.log ("let c with pparseFloat " + parseFloat (c));
console.log ("let d with parseFloat " + parseFloat (d));

//using Number for each variable and displaying the results

console.log ("let a with Number " + Number (a));
console.log ("let b with Number " + Number (b));
console.log ("let c with Number " + Number (c));
console.log ("let d with Number " + Number (d));

//using Unary for each variable and displaying the results

console.log ("let a with Unary " + (+a));
console.log ("let b with Unary " + (+b));
console.log ("let c with Unary " + (+c));
console.log ("let d with Unary " + (+d));
